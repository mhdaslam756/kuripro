/** Test stub for the vite-plugin-pwa `virtual:pwa-register` module (aliased in vitest.config.ts). */
export function registerSW(_options?: unknown): (reload?: boolean) => Promise<void> {
  return () => Promise.resolve();
}
